# Shared UI Module
## Boundary
Reusable atomic UI components and utilities that are not tied to any specific feature domain.

Contains:
- Shadcn/UI component implementations
- TailwindCSS-based primitive components
- UI utility functions and helpers
- Global animation presets

## Rules
Zero feature dependencies. All components must be pure and fully reusable across all features.
