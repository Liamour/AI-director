# Global Store
## Boundary
Zustand global state management slices.

Contains:
- Global state slices
- State persistence logic
- Cross-feature state selectors
- Store initialization

## Rules
Feature-specific state should remain in feature directories unless explicitly needed globally.
